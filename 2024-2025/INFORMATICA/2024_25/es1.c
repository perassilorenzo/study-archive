/*
int a, b, c;
a = 0;
b = 1;
c = a + b;

if //condizione
    //azione per il vero
    ;
else //azione per il falso
    ;


//input
scanf(//argomenti per la lettura dei dati in ingrasso
      );

//output
printf(//argomenti per la scrittura dei dati in uscita
       );

//salto
for (//argomento per il funzionamento del ciclo
     );
----------
while (//condizione
       )
       //nucleo
       ;
do

    //nucleo;
while (//condizione
       );

//inizio
int main()

//fine
return0()

*/

#include <stdio.h>

int main()
{
    float a, b, media;
    printf("inserisci i due numeri: ")
    scanf("%f &f", &a, &b);
    media = (a+b)/2;
    printf("la media è, %f", &media);
    return 0;

 }

